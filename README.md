# PyNNW
this project opensorce this look like code a pytorch but this make for create a neural network
this is how to use for make neural network FCNN
```python
import pynnw as pnn
model = pnn.NNW([4,8,2])

x = pnn.array([[0.4,0.8,0.3,0.9]])
print(model.forward(X))
```
